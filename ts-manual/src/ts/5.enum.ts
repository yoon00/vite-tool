/* enumerable type */

enum Direction {
    UP,
    DOWN,
    LEFT,
    RIGHT
}

const direction = {
    up:Direction.UP,
    down:Direction.DOWN,
    left:Direction.LEFT,
    right:Direction.RIGHT
}

enum End_point {
    USER = 'https://naver.com',
    POKEMON = 'https://pokeapi.co/api/v2/pokemon'
}

const END_POINT = {
    user:End_point.USER
}